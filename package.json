{
  "name": "@cmdsree/safe-env",
  "version": "1.0.0",
  "description": "Safe environment variable helpers.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/index.d.ts",
    "dist/index.js",
    "README.md"
  ],
  "scripts": {
    "build": "tsc",
    "prepack": "npm run build",
    "pretest": "npm run build",
    "test": "node --test dist/*.test.js",
    "test:coverage": "npm run build && c8 --check-coverage --include dist/index.js --lines 95 --branches 90 --functions 100 node --test dist/*.test.js",
    "release": "semantic-release"
  },
  "keywords": [
    "env",
    "environment",
    "config"
  ],
  "author": "",
  "license": "MIT",
  "type": "commonjs",
  "devDependencies": {
    "@types/node": "^25.6.2",
    "c8": "^11.0.0",
    "semantic-release": "^25.0.3",
    "typescript": "^5.4.0"
  }
}
